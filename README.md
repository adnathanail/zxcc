## ZX Diagram InfoView widget

React widget which renders ZX diagrams

Uses Pyodide (running Python in browser with WASM) to render diagrams using PyZX

Bundles Pyodide WASM files into the build, so they don't need to be downloaded when running

Currently installs Python deps at runtime